# The GitHub Pages build: no backend, no API -- read the JSON artifacts
# published alongside the static site (web/public/data/, written by
# `agx_research.cli export-dashboard`). See docs/ARCHITECTURE.md.
VITE_DATA_PROVIDER=static

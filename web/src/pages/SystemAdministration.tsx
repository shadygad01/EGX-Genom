import { ComingSoon } from "./ComingSoon";

export function SystemAdministration() {
  return (
    <ComingSoon
      title="System Administration"
      detail="Runtime configuration, execution history, replay, artifacts, versions, and performance. Next milestone."
    />
  );
}

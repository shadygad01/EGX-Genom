# Technical Debt Register

Known, deliberate debts — each with why it's acceptable now and what
triggers repayment. Anything not listed here and discovered later should
be added, not silently fixed or silently tolerated.

| # | Debt | Why acceptable now | Repayment trigger |
|---|------|--------------------|-------------------|
| 1 | `KnowledgeObject` inherits its `Hypothesis`'s id (Epoch I contract); the graph skips the resulting self-loop edge in pipeline projection. | Stable contract; ids stay traceable. | Any consumer needing hypothesis and knowledge as distinct graph nodes → introduce distinct knowledge ids in a 2.0 schema migration. |
| 2 | `ProvenanceRef.kind` is a free string informally synced with `graph.NodeType`; unrecognized kinds silently skip edge creation (documented in `edges_from_provenance`). | Every current kind is a NodeType member; skip-not-mislabel limits damage. | First bug traced to a typo'd kind → shared enum + conformance test. |
| 3 | JSON-file repositories are in-memory, single-writer, fully rewritten per `add()`. | Correct and fast at current scale; interface hides it. | Data volume or concurrent writers → database-backed `Repository[T]`. |
| 4 | Pairwise feature search is O(n²) in universe size (435 pairs at EGX30). | Trivial at 30 tickers. | Universe expansion toward all listed companies → candidate pre-filtering/caching. |
| 5 | `NaiveDirectionalBacktester` ignores transaction costs and sizing (stated in its own notes). | Honest gate on directional evidence; costs need a portfolio-level harness. | Portfolio optimizer work (post-1.0) → cost-aware backtest replaces the gate threshold defaults. |
| 6 | Conflict-policy confidence constants (corroboration boost 0.5, dispute penalty 0.2) and ontology impact-horizon mapping are defensible defaults, not calibrated. | No multi-source or long-horizon real data exists to calibrate against. | First real second data source / first year of real events → calibration study. |
| 7 | `PeerValidatorReviewer` replication uses the same snapshot (perturbed methodology only). | The only data that exists is one snapshot; perturbation is the honest replication available. | Real multi-day history → replicate on disjoint periods. |
| 8 | Pipeline `PipelineConfig.extra_agents` field is currently unused. | Harmless config surface. | Next pipeline change → remove or wire. |
| 9 | `api/`/`web/` expose knowledge/events/patterns/recommendations/market-state/runtime-metrics/system-status/source-registry, but sessions/genes/papers/portfolios still have no HTTP surface. | Epochs II+ were explicitly scoped Python-core-only; the dashboard dual-provider work (see docs/ARCHITECTURE.md) added the eight resources the dashboard actually needs. | First presentation-layer requirement for genes/papers/portfolios/sessions → extend `dashboard/export.py` + the matching API route + regenerate contracts. |
| 10 | Monkey-test note: `learning` monitor uses each knowledge object's *primary* asset only for realized performance. | Single-asset attribution is the defensible v1; pair claims are directional on the primary. | Portfolio-level attribution work → per-claim realized-return definitions. |
| 11 | `collectors.quality.assess_quality()`'s `consistency_score` is always `None` in practice — no two `IMPLEMENTED` sources yet cover overlapping data to corroborate against. | Only one source per data type (Stooq for prices, FRED for macro, RSS for news) is collectable today; a guessed consistency score would violate "never fabricate." | A second `IMPLEMENTED` source overlapping an existing one (e.g. a second price feed) → wire real cross-source comparison. |
| 12 | `RssNewsCollector` stores only headline/link/date per entry (`NewsItem.body` holds the original URL, not article text). | Full-text extraction is a per-outlet scraping concern the "no scraping shortcuts" rule explicitly defers until a source is reviewed and its terms confirmed to allow it. | A specific outlet's `TOS_REVIEW` clears → outlet-specific full-text extension, not a change to the generic collector. |
| 13 | ~~`sources.SourceRegistry.record_measured_quality()` exists but nothing calls it yet~~ **Closed**: `collectors.service.CollectionService` now calls it on every run via `sources.reputation.compute_reputation()`. | — | — |
| 14 | `api/`'s `ArtifactsReader` (patterns/recommendations/market-state/system-status/source-registry) reads pre-generated snapshot files rather than recomputing live — nothing refreshes `DASHBOARD_ARTIFACTS_DIR` on a schedule in production yet. | No deployment target exists yet (System 18); `agx export-dashboard` is proven correct (same code path GitHub Pages uses) and just needs a periodic trigger once one does. | First real deployment → cron/timer running `agx export-dashboard` against the live `--data-dir`. |
| 15 | `patterns.json` has no pydantic schema in `contracts/` — it's typed `never[]` in TS and validated as "always empty" rather than against a real model. | **Partially closed**: `agents.historical_patterns.HistoricalPatternsAgent` is now implemented and its findings flow through the normal finding/hypothesis/knowledge pipeline like any other agent's — it is not blocked. `patterns.json` specifically is a separate, still-undesigned dashboard display artifact (raw matched episodes, not pipeline findings); inventing that schema now would still be guessing a UI shape nothing has asked for yet. | A concrete UI need for a raw-pattern display (as opposed to the agent's findings, already visible via the normal knowledge/recommendation surfaces) → real `Pattern` pydantic model, contract, and TS type replace the always-empty placeholder. |
| 16 | ~~`sources.reputation.compute_reputation()`'s `latency` dimension is always `None` in practice~~ **Closed**: `HttpFetcher.fetch_bytes` now times each successful `urlopen`+`read` (excluding rate-limit/backoff sleeps) into `self.request_latencies`; `CollectionService.run()` reads the new entries appended during each `Collector.fetch()` call and passes their average into `SourceMetricsRepository.record_run(latency_seconds=...)`. Still `None` in practice until a real (non-mock/replay) `HttpFetcher`-backed collector actually runs — an honest absence, not a placeholder. | — | — |
| 17 | `sources.qualification`'s stage thresholds (min runs per stage, 0.70/0.85 composite cutoffs) and `sources.health`'s alert thresholds (3 consecutive failures → DOWN, staleness floor 0.3) are declared policy, not calibrated. | No source has accumulated real run history yet to calibrate against (same situation as TD-6's conflict-policy constants). | First source with substantial real run history (post-deployment) → calibration study, likely alongside TD-6's. |
| 18 | ~~`discovery.engine`'s heuristics only see already-fetched HTML/XML text — they can't find feeds/APIs/PDFs that a page only reveals via client-side JavaScript, and `discover_sitemap_urls` doesn't recurse into a sitemap-index pointing at other sitemaps.~~ **Partially closed**: a live run evidenced the exact "reachable homepage, real content, nothing discoverable" gap this predicted (Zawya, see `docs/ACQUISITION_STRATEGY.md`). `AcquisitionIntelligenceEngine.run_for_target()` now falls back to robots.txt's `Sitemap:` directive / the conventional `/sitemap.xml` path when `scan_page()` finds nothing, follows a sitemap-index one level, and classifies sitemap entries by file extension instead of blanket `HTML_SCRAPE` — all real, verifying fetches through the existing injected `fetch_text`, never a fabricated URL. **Still open**: JS-rendered-only content remains invisible to this engine (no headless-render step exists). | No discovery run has hit the JS-rendering half of this limitation yet; every heuristic implemented so far is a real, working generic scanner over static markup (now including sitemaps). | A verified target site that's JS-rendered → add a headless-render step ahead of the existing heuristics. |
| 19 | `collectors.archive_replay.ArchiveReplayCollector` accepts a `real_collector` and copies its `fetcher` but never calls it — dead reference kept only so `Collector.fetcher` stays populated for anything downstream that inspects it. | Harmless; replay's `fetch()` reads from the raw archive, not the network, by design. | Any future code path that expects a replay collector's `fetcher` to be meaningfully used → reconsider whether it belongs on `Collector` at all. |
| 20 | `acquisition_intelligence.legality`'s red/green ToS-keyword flag lists are a starting heuristic, not calibrated against a real corpus of terms-of-use pages. | No real ToS page has been checked yet (no network egress in this sandbox); the lists are defensible starting phrasing, same honesty tier as the seed catalog's declared priors. | First batch of real `agx discover-sources` runs against real ToS pages → review false positives/negatives and expand the lists, or replace with a more structured ToS classifier. |
| 21 | `acquisition_intelligence.domain_resolution.HeuristicDomainResolver`'s fallback TLD list (`.com`/`.org`/`.net`, plus `.com.eg`/`.gov.eg` for Egypt) is small and bounded — a target with no `domain_hints` and an uncommon TLD (e.g. `.press`, `.info`, `.media`) won't resolve. | Every seeded `TargetOrganization` currently has `domain_hints`, so this path is untested against a real gap; a bounded guess set is deliberately conservative rather than an open-ended crawl. | A target added with no hints fails to resolve in a real run → expand the fallback TLD list. A keyed search-API-backed resolver is **not** an option per the project owner's no-`NEEDS_KEY`-sources decision (see `docs/DATA_ACQUISITION.md`). |
| 22 | `acquisition_intelligence.live.build_live_wayback_client`'s `fetch_json` swallows every fetch failure (timeout, bad JSON, network error) into `{}`, which `historical.py` then reads identically to "no snapshot found." | A genuine Wayback API outage during a live run would understate `HistoricalAvailabilityAssessment.score` rather than crash or retry — acceptable for a best-effort signal that's one of several inputs to ranking, not gating on its own. | First observed case where this masks a real signal in production → add a third state (`checked=False`) distinct from `has_any_snapshot=False`, mirroring `legality.py`'s `robots_allows: bool \| None`. |
| 23 | `AcquisitionContinuityMonitor` only reacts to a source already gone `HealthStatus.DOWN` — it never proactively re-discovers alternatives for a healthy source stuck at low reputation/`QUARANTINE`. | Reactive recovery is the charter's literal ask ("continue researching alternative acquisition methods whenever one becomes unavailable"); proactive improvement is a natural extension, not the stated requirement. | **Partially closed**: System-18 scheduling now exists — `.github/workflows/discovery.yml` runs a periodic full-catalog `agx discover-planned-report` pass weekly (see `docs/DATA_ACQUISITION.md`'s "Discovery workflow" section) — but it covers only `PLANNED`/`CANDIDATE` sources, per the project owner's explicit scope for that workflow. `AcquisitionContinuityMonitor.check_and_recover()` (DOWN-triggered recovery) is still never called from a schedule; wiring it alongside the weekly pass remains open. |
| 24 | ~~`production.collector_plan`'s mock/replay fixtures don't include corporate events~~ **Closed**: `CollectionBatch` gained `corporate_events`; `CollectionService` materializes them to `corporate_events.csv` (merged by ticker+date+event_type, same idempotency pattern as price bars); `RssNewsCollector`'s new `classify_corporate_events` flag (headline keyword heuristic, see TD-29) populates them, wired into `collector_plan.py`'s `rss_generic` mock/replay collector. Verified live: the production pipeline's mock run now writes real `COMI/EARNINGS` and `MFPC/DIVIDEND` rows from the existing mock RSS headlines. Remaining gap, tracked separately: a real Company IR PDF disclosure source (see `docs/DATA_ACQUISITION.md`) would give richer, numeric-detail events than a headline classifier ever can — not blocking, since headline-classified events are correctly informational-only (`details={}`) until then. |
| 25 | `ProductionPipeline._stage_market_memory` calls `MarketMemory.reconstruct()` once for its own reporting checkpoint, and `RuntimeEngine.run_range` (inside the Research Pipeline stage) reconstructs the same state again per day — redundant work, though harmless since `EventPlatform.register_all` is idempotent by fingerprint. | Keeping Market Memory as its own reportable stage (matching the mission's exact stage list) was judged more valuable than eliminating one extra, cheap reconstruction; `DailyResearchPipeline`/`RuntimeEngine` were not modified to avoid redesigning a completed system. | Performance profiling ever shows `reconstruct()` itself as expensive (real vendor data, large snapshots) → have the Market Memory stage's already-built `MarketState` flow directly into the Research Pipeline stage instead of re-deriving it. |
| 26 | `ProductionPipeline`'s "Knowledge Base" and "Genome" stages are reporting checkpoints (before/after counts) around the Research Pipeline stage's mutation, not independent mutation steps — gene creation and knowledge promotion are one atomic flow inside `DailyResearchPipeline`. | Splitting that flow into separate Knowledge Base / Genome mutation steps would mean reimplementing part of `orchestration.pipeline.DailyResearchPipeline`, which the mission explicitly rules out ("do not redesign completed systems"). | If a future charter change genuinely decouples knowledge promotion from gene creation (e.g. multiple genes per knowledge object) → revisit whether these need to become real independent stages. |
| 27 | `production.collector_plan.build_collector_plan`'s wireable source set (`stooq`/`fred`/`rss_generic`/`worldbank`) is a hardcoded list inside the production pipeline, not derived automatically from `SourceRegistry.collectable()`. | Explicit wiring matches the platform's "small adapter, not auto-guessed" philosophy — a newly-`IMPLEMENTED` source needs a human/engineer to decide *how* the pipeline should configure it (which tickers, which series), not just that it exists. | A new source goes `IMPLEMENTED` (e.g. via the Acquisition Intelligence Engine once it has network egress) → add its wiring to `collector_plan.py` explicitly; this is the intended "small adapter" step, not debt to eliminate. |
| 28 | `discovery.engine.discover_company_directory_links()`'s anchor-text match (normalized token subset, with a fixed stopword list) is a starting heuristic, not calibrated against a real EGX directory page — a company whose registered name diverges significantly from its anchor text on EGX's site (abbreviation, transliteration variant, listed subsidiary name) won't match. | No real EGX directory page has been fetched yet (no network egress in this sandbox); the heuristic is deliberately conservative (subset match, not fuzzy/edit-distance) to avoid a false positive feeding a wrong domain hint into `generate_company_ir_targets`. | First real `agx discover-sources` run against EGX's actual directory page → review match/miss rate per company and expand the stopword list or add a fuzzy fallback (never a fabricated hint) for misses. |
| 29 | `collectors.corporate_event_classifier.classify_corporate_event_type()`'s keyword-to-event-type mapping is a declared starting heuristic (same posture as TD-20/TD-28), not calibrated against a real corpus of EGX corporate disclosure headlines — some real headlines will match nothing (`None`, correctly non-fabricating) or match the wrong category (e.g. a headline mentioning "dividend" in passing while announcing something else). | No real IR/news headline corpus exists yet to calibrate against; the classifier is headline-only by design (RSS/ToS terms, TD-12) so it can never extract numeric details (split ratio, dividend amount) regardless of calibration — classified events stay correctly informational-only (`details={}`). | First batch of real IR/news headlines collected → review precision/recall per category and expand/prune the keyword list; a richer source (IR disclosure PDF) is the real fix for numeric detail, not better headline keywords. |
| 30 | `collectors.index_constituents.IndexConstituentCollector`'s ticker/name column detection (header-text matching for "ticker"/"symbol"/"code" and "name"/"company") is a starting heuristic, not verified against a real EGX constituent-list export — the exact column headers, delimiter, and encoding EGX actually publishes are unknown (no network access to check). | Inventing a parser for an unverified specific format would be guessing a wire format, not parsing a real one — the header-matching approach is deliberately format-tolerant rather than hardcoding assumed column names. | First real fetch of EGX's constituent-list endpoint (once `egx_official` is verified and flipped to `IMPLEMENTED`, `AD-24`) → confirm the collector parses it correctly, or adjust the header hints / add a non-CSV parse path if the real format differs. |
| 31 | `collectors.financial_statements.FinancialStatementCollector`'s column detection (header-text matching for period-end/period-type/statement-type/line-item/value/currency) is a starting heuristic, not verified against any real structured financial-statement export — same posture and same reason as TD-30. | No real company IR or data-vendor financial-statement export has been fetched yet to confirm a header shape. | First real structured financial-statement export fetched (once a concrete `company_ir` or vendor `SourceSpec` is verified and `IMPLEMENTED`) → confirm parsing, or adjust column hints if the real format differs. |
| 32 | ~~No PDF-based financial-statement extraction exists~~ **Partially closed (2026-08-03, real financial-statement coverage gap review)**: `collectors.company_earnings_table.CompanyEarningsTablePdfCollector` (`parse_earnings_summary_table`) is a real, tested `PdfDocumentCollector` subclass, built only after inspecting real extracted text from two real, already-discovered PDFs (RMDA's and TMGH's 1Q26 earnings releases, fetched live via GitHub Actions run `30810863486` — this sandbox has no network egress of its own). It reads only a company's own structured "`EGP mn <period> <period> ...`" summary table, never prose (real evidence showed prose phrasing differs company to company, so a single generic regex doesn't generalize), and only ever records a fixed, evidenced label whitelist (`revenue`/`gross_profit`/`ebitda`/`operating_income`/`net_income`/`recurring_net_income`) — a structurally identical but unrelated table elsewhere in the same real document (RMDA's per-channel Sales/Volumes breakdown; TMGH's per-segment revenue rows) is never mistaken for it. Wired as real, `IMPLEMENTED` `SourceSpec`s `rmda_ir`/`tmgh_ir` (`sources.catalog`), in `CAPABILITY_STRATEGIES[FINANCIAL_STATEMENTS/INVESTOR_RELATIONS]`, `production.collector_plan.build_live_collector`, and `production.decision_lineage.DECISION_ROUTES`. Tests use the real extracted text as fixtures (`tests/fixtures/{rmda,tmgh}_1q26_earnings_real_extract.txt`), not synthetic data. | The remaining 6 real, live-discovered Family B PDF sources (JUFO, ORWE, EAST, ADIB, CCAP, ORHD — see `docs/COLLECTOR_TEMPLATE_TAXONOMY.md`) were inspected the same session but not wired: their earnings releases are prose-only in the fetched excerpt (no summary table found), a scanned/image-only PDF was directly confirmed for one of ORWE's two documents (0 extractable chars), and a per-company prose parser risks exactly the "silently reading the wrong figure" failure this debt was created to avoid — building one blind, without inspecting each company's real text first, would repeat the original mistake this entry exists to prevent. | Fetch and inspect each remaining company's real extracted text (reusing `research/scripts/probe_pdf_text.py` against a real GitHub Actions run) before writing or extending any parser for it; extend `_LABEL_MAP`/`_HEADER_RE` only after a new label/header shape is confirmed against real text, never by guessing a company's likely phrasing. |
| 34 | ~~`ticker_data_gap_report.json` had no web/API surface~~ **Closed**: `api/src/artifactsStore.ts`/`api/src/routes/dashboard.ts` (`GET /ticker-data-gap-report`), `web/src/data/{DataProvider,ApiProvider,StaticJsonProvider}.ts`, and `web/src/types.ts` (`TickerDataGapReport`/`DataLayerGap`) now mirror `financial_statements.json`'s exact wiring. UI surface: Opportunity Center's existing "Decision Readiness" table gained `onRowClick`, paired with a new "Data Coverage" detail card (the same click-to-select-plus-detail-panel pattern the Opportunities table above it already uses) showing the 5-layer completeness breakdown, blockers, and next actions for the selected ticker. | — | — |
| 33 | `acquisition_intelligence.capability_engine.rank_capability_strategies()`'s composite formula (mean of declared reliability/freshness, evenly blended with measured reputation once any exists, `conflict_priority` folded in at 1/1000th scale to break close ties) is a declared starting weighting, not calibrated against real cross-capability outcomes — same posture as TD-6/TD-17/TD-20. `CAPABILITY_STRATEGIES`' candidate pools (`acquisition_intelligence/capability.py`) are also a starting, documented set per capability, not exhaustive — a legal strategy this catalog doesn't yet name isn't automatically considered. | No capability has enough real run history yet (across more than one `IMPLEMENTED` candidate) to calibrate relative weighting against; the seed catalog itself only has one `IMPLEMENTED` candidate for most capabilities today (see `docs/ACQUISITION_STRATEGY.md`). | A capability accumulates two or more `IMPLEMENTED` candidates with real run history (e.g. a second live price or macro source) → review whether the declared weighting actually orders them the way measured outcomes would. |
| 35 | `agents.news_sentiment.classify_headline_sentiment()`'s positive/negative phrase lists are a declared starting heuristic, not calibrated against a real corpus of EGX news headlines — same posture as TD-20/TD-28/TD-29. Headline-only by design (RSS/ToS terms, TD-12), so it can never read tone from full article text regardless of calibration. | No real Egyptian news headline corpus with known outcomes exists yet to calibrate against; `enterprise_press`/`fra_egypt` are only weeks into real live collection. | A meaningful batch of real live headlines accumulates → review precision/recall of the phrase lists against subsequent price behavior, expand/prune, or replace with a more structured sentiment classifier. |
| 36 | `universe.entity_resolution.resolve_ticker_mentions()` has no Arabic alias list — only the ticker code and the English display name from `research/data/universe/EGX30.csv`/`EGX70.csv` are matched. A headline that names a company only in Arabic, or by a common English abbreviation not present in the EGX-sourced display name, won't resolve. | No verified Arabic-language source for EGX30/EGX70 constituent names exists in this codebase (the seed CSVs' own `source_url` points at EGX's English listing page only); inventing Arabic transliterations from training-data recall risks a *wrong* alias, which is worse than a missed match. | A verified Arabic-language EGX constituent listing (or another cited, real source) is fetched → add an `aliases: dict[str, list[str]]` input to `resolve_ticker_mentions()` and populate it from that real data, never a guessed transliteration. |
| 37 | `data.point_in_time.ASSUMED_PUBLICATION_LAG_DAYS`'s per-source publication-lag floors (worldbank/undata/capmas: 30 days, fred: 0) are a declared, deliberately conservative *floor*, not a measured or cited real average lag per source — same posture as TD-6/TD-17/TD-20. Picked low enough to never contradict this codebase's own live-verified evidence (a real World Bank observation confirmed collectible ~165 days after its period end), not derived from a real publication-schedule citation. | No verified per-source publication-schedule source has been consulted; this codebase has never collected real publication metadata (only period-end dates) to calibrate against. | A verified publication-schedule source is consulted (e.g. World Bank's/CAPMAS's own stated release calendar) → replace the floor with a cited, source-specific figure per series where it differs materially. |
| 38 | `discovery.web_search_hints.load_web_search_domain_hints()` reads a reviewed, evidenced JSON snapshot (`research/data/universe/egx30_web_search_domain_hints.json`, 26/31 EGX30 tickers, gathered via live web search — not training-data recall) as a third `domain_hints` source for `generate_company_ir_targets()`, alongside `discovery.wikidata_lookup` and `discover_company_directory_links()` (TD-28). It is a point-in-time snapshot, not a live lookup (no network egress existed in the sandbox that produced it), and covers EGX30 only — EGX70 and re-verification are unrun. Five EGX30 tickers (EGCH, HELI, MCQE, OIH, PHDC) were deliberately left unresolved rather than guessed — see the file's own `unresolved_no_confident_hint`. | Wired into `cli.py`'s `discover-sources` (applied only where Wikidata has no hint, since Wikidata's structured `P856` claim is preferred when both exist) and unit-tested, but never run live end-to-end: `HeuristicDomainResolver`'s actual reachability probe, and downstream candidate discovery/legality/stability/historical scoring, still need a real `agx discover-sources` run in an environment with network egress (this sandbox's egress policy blocks arbitrary external hosts, confirmed live against egx.com.eg/cibeg.com/fawry.com/telecomegypt.eg/wikidata.org/archive.org). | First real `agx discover-sources` run with network egress → confirm how many of the 26 hinted hostnames actually resolve/qualify; extend the snapshot to EGX70; revisit the 5 unresolved tickers if a confident source turns up. |
| 39 | `discovery.company_financial_registry`/`company_financial_discovery` (new: per-company Financial Source Registry — Investor Relations/annual/quarterly/financial-statements links, source type, collector recommendation) plus `scripts/build_financial_source_registry.py` (resumable EGX30+EGX70 runner, wired into `.github/workflows/discovery.yml` alongside the existing weekly discovery job) are real, tested code, but a real end-to-end run of that script inside this sandbox produced **0 `DISCOVERED`/`VALIDATED` and 101/101 companies either `BLOCKED` (26, all 26 hinted EGX30 tickers from TD-38 — real fetch attempts, each failed with the identical egress-policy `403` TD-38 already evidenced) or `HOMEPAGE_UNRESOLVED` (75 — 70 EGX70 tickers plus the 5 EGX30 tickers TD-38 left unresolved, no evidenced homepage hint exists for any of them yet)**. `classify_financial_document()`'s keyword lists (annual/quarterly/financial-statements/investor-relations) are also an uncalibrated starting heuristic — same posture as TD-28/TD-29 — since no real company IR page has ever been fetched to check against. | No network egress in this sandbox (same evidenced block as TD-38) to fetch even the 26 companies with a known hostname, and no domain hint at all yet for the other 75 (EGX70 web-search pass and the 5 EGX28 unresolved tickers are still open — see TD-38's own repayment trigger). Marking any of these `VALIDATED` without a real fetch confirming real documents would be exactly the fabrication this program's rules forbid — the registry honestly reports "not yet resolved," not invented content. | First real `.github/workflows/discovery.yml` run (weekly, or manual `workflow_dispatch`) with real network egress → confirms how many of the 26 `BLOCKED` companies move to `DISCOVERED`, calibrates `classify_financial_document()`'s keyword lists against real IR-page anchor text/URLs, and the resumable script naturally re-attempts everything not yet `VALIDATED` on every subsequent run. EGX70 hint coverage is the same trigger named in TD-38. |
| 40 | ~~The daily production pipeline (`deploy-pages.yml`'s `agx run --mode live` step) has no cross-run persistence~~ **Closed**: `deploy-pages.yml` now restores `research/data/production/` from a `production/state-latest` branch before running (`--data-dir data/production`, replacing the old ephemeral `/tmp/agx_dashboard_data`), auto-commits/force-pushes it back after a successful, dashboard-validated run (never PR-gated — operational state, not reviewed evidence), and skips the whole rest of the job (research pipeline, dashboard build, Pages deploy) via a `RunRecordRepository`-backed same-date check when today's date already succeeded in the restored state, so a same-day double-trigger (e.g. `schedule` + a same-day `push`) can't duplicate hypotheses/knowledge for one date. Real repo-growth (`raw_documents.json` keeps every fetched payload's text) is an accepted, disclosed tradeoff of this fix, not an oversight. | — | Watch actual repo size growth over the coming weeks/months; if `raw_documents.json`/the raw archive grow unmanageably, consider a retention/pruning policy (e.g. archive-and-truncate older raw payloads whose provenance is already captured) rather than reverting persistence. |
| 41 | `GdeltDocCollector`'s historical backfill mode (`start_date`/`end_date`/`window_days`) and its `.github/workflows/news-history-backfill.yml` runner are real, tested code (windowed `startdatetime`/`enddatetime` queries, one per `window_days` slice, going around GDELT DOC 2.0's 250-articles-per-response cap), but materialize into their own `research/data/news_history/news.csv`, reviewed via PR into `main` like `discovery.yml`'s evidence — a deliberately separate directory from `research/data/production/` (debt #40's now-persisted production data-dir), not yet merged in. The workflow's first real run (2026-07-29) silently collected nothing: `--data-dir` was placed after the `collect` subcommand instead of before it (a global `argparse` argument, not `collect`-specific — `cli.py`'s own top-level `parser.add_argument("--data-dir", ...)` is added before `add_subparsers()`), and the resulting `agx: error: unrecognized arguments` was swallowed by an unguarded `\| tee` (no `pipefail`), so the step reported "success" despite doing nothing. Fixed (argument order corrected; `set -o pipefail` added) and the fix is confirmed real: the second live run (2026-07-29, run #2) surfaced a genuine failure instead of a silently-swallowed one — GDELT's own API returned **HTTP 429 Too Many Requests on the very first windowed request**, all 3 retry attempts (2s/4s backoff) still 429, so the run correctly failed loudly rather than reporting false success. This is not our own `RateLimit`/`min_seconds_between_requests` throttle firing (that only gates the interval *between* separate requests to the same source, and this was the first request of the run); it looks like either GDELT rate-limiting/blocking the shared GitHub Actions runner IP range itself, or a stricter real quota on bulk historical `startdatetime`/`enddatetime` queries than the 5 req/min this codebase assumed for its 7-day daily-live query. A third live run (2026-07-29, run #3) refined the evidence: window 1 (the first request of the run) succeeded this time, and window 2 was the one that hit 429 — proving it isn't an outright IP block, but a real per-request-cadence throttle stricter than the 5 req/min this codebase had assumed, and also exposing a real design gap: `fetch()` collected every window into one list and only returned it at the end, so one failed window (even after a prior success) discarded every already-fetched window's real data. Both addressed: `GdeltDocCollector`'s historical mode now catches a per-window failure, records it in `self.fetch_warnings` (same posture as `FredCsvCollector`'s per-series resilience), and continues to the next window — raising only if every single window fails — so a real mid-batch 429 no longer discards already-collected news; and the `gdelt` `SourceSpec`'s `RateLimit` widened from 5 req/min (12s) to 2 req/min (30s), a bounded adjustment motivated directly by the observed failure point, not a blind guess. | GDELT's real production behavior under a bulk historical query burst from a shared cloud CI IP was unknown until live runs evidenced it twice; the per-window resilience fix and the widened rate limit are both responses to that concrete evidence, not speculation. | A fourth run succeeded: 62/104 windows, 14,984 real articles, opening a `news-history/latest` PR. But inspecting the actual content surfaced a bigger, separate finding (project owner direction, 2026-07-29): only ~3.8% of headlines even mentioned "Egypt" -- the plain-OR query was far too loose. Resolved by (1) tightening the daily-live and backfill default query to require an Egypt term AND a finance term (`production/collector_plan.py`, workflow input default), and (2) a structural fix, not just a query fix: GDELT is now a DISCOVERY-tier source (`sources.spec.EvidenceTier`) that can never independently write to `news.csv` or register an Event -- `collectors.discovery_reconciliation.reconcile_discovery_news()` is the only promotion path, requiring an independent PRIMARY source to report the same ticker nearby in time first (see debt #43 for the full design and debt #42 for a ticker-collision bug this reprocessing evidenced). The already-collected batch was refiltered to 284 genuinely Egypt-relevant headlines and re-pushed to the `news-history/latest` PR as `news_discovery.csv` (not `news.csv`). | Closed as designed: GDELT collection is real and working; the evidence-quality gate (debt #43) is what now decides whether any of it ever reaches agents. Watch `reconcile-discovery-news`'s promoted/still-pending counts over time as PRIMARY-source coverage (Enterprise/FRA/Al Borsa/Masrawy/company IR) grows. |
| 42 | `universe.entity_resolution.resolve_ticker_mentions()` false-positives on tickers that collide with common English words or short abbreviation sequences. Re-resolving the real historical GDELT batch (TD-41, 13,463 real headlines) against the real EGX30/EGX70 ticker list found 137 "matches", of which 137 (100%) were false positives: `EAST` (Eastern Company) matched 84 headlines about the Middle East region; `ARAB` (Arab Developers Holding) matched 53 headlines about Arab countries/organizations; `TMGH` (T M G Holding) matched one unrelated Hungarian-language headline via `significant_tokens()` reducing "T M G Holding" to single-letter tokens that spuriously overlapped the text. Not a synthetic example -- a real, reproducible failure mode discovered by this reprocessing, not a hypothetical. | The exact-token/company-name-token matching design (TD-36's word-boundary fix) already closed the "VLMR inside VLMRA" substring false-positive; short/common-word tickers and short company-name abbreviations are a different, still-open false-positive class the same design doesn't address. Guessing at a fix (a stoplist of "risky" tickers, a minimum-token-length rule) without evidence of which tickers are actually risky would repeat the same mistake TD-36 fixed. | A verified list of which real EGX30/EGX70 tickers/abbreviations collide with common English words (EAST, ARAB confirmed; audit the rest of the universe) → either exclude bare-ticker-code matching for those specific tickers (require the full company name instead) or require additional context (e.g. a nearby EGX/stock-market term) before trusting a short/common-word ticker match. Until then, `reconcile_discovery_news()`'s ticker-based matching inherits this risk for any future discovery candidate whose headline happens to use one of these words unrelated to the company. |
| 44 | `agents.financial_performance.FinancialPerformanceAgent`'s growth/leverage shift thresholds (`min_growth_shift=0.05`, `min_leverage_shift=0.15`) are a declared, uncalibrated starting posture — same class as TD-6/17/20/33. | No real ticker has ≥4 collected financial-statement periods yet (only `telecom_egypt_ir`/`orascom_ir` are `IMPLEMENTED`, each with 1-2 real periods) — no real outcome history exists to calibrate against. | Once ≥4 real periods exist for at least one ticker and enough decision-ledger history accumulates → review whether these thresholds actually separate signal from noise. |
| 45 | `decision_service.country_risk.assess_country_risk()`'s `deterioration_threshold=0.05` and `rating_action_lookback_days=180` are declared, uncalibrated — same posture as TD-6/17/20/33/44. | No real multi-year EGP crisis history or rating-action feed exists in this platform yet to calibrate against. | Once `moodys_ratings`/`sp_global_ratings`/`fitch_ratings` collect real history (see TD-48) and/or a real EGP stress period is observed → review both constants against what actually preceded/followed real severity events. |
| 46 | `decision_service.liquidity_floor.DEFAULT_MIN_AVERAGE_TRADED_VALUE` (1,000,000 EGP average daily traded value — one shared constant `meta.readiness` and `cli.py`'s `decide` command both import, never a redeclared copy) is a declared, uncalibrated floor — same posture as TD-6/17/20/33/44/45. Chosen only to clear COMI/MFPC's real mock traded value (tens of millions EGP/day) comfortably, not derived from a real EGX liquidity study. | No real multi-year EGX liquidity history exists yet to calibrate a real "can this size actually trade without moving the price" threshold. | Once real trading data accumulates across more of the universe (not just COMI/MFPC) → review whether this floor correctly separates genuinely thin names from liquid ones. |
| 47 | ~~`decision_service/` is built and tested standalone but not yet wired into `production/pipeline.py` or exposed as a queryable `cli.py` command~~ **Closed**: `agx decide --date ... [--positions positions.json]` (`cli.py`) reconstructs a real `MarketState` from `--data-dir` (`LocalCsvDataProvider`/`CollectedUniverseProvider`/`CollectedFinancialStatementProvider`, mirroring `ProductionPipeline`'s own construction), reads `PositionState`s from an optional JSON file (a ticker absent from it is treated as not held), builds `Recommendation`s via the existing `RecommendationService`, and calls `DecisionService.decide_portfolio()` with a real `assess_country_risk`/`compute_illiquid_tickers` read — printing the resulting `PositionAwareDecision`s as JSON, exit code 0. Deliberately read-only and on-demand (not wired into `ProductionPipeline`'s stage list, per `AD-45`/R-24) -- following `agx publication-status`'s exact pattern. Verified live (mock mode): an unheld universe with no promoted knowledge returns `[]`; a held ticker with no fresh INVESTMENT-horizon evidence correctly abstains to `hold`, `target_weight=0.0`, with a full `Explanation`/`Provenance`. 2 new CLI tests (`test_cli_decide.py`); 734 backend tests pass. | — | — |
| 49 | ~~A real live production deployment (2026-07-31, `deploy-pages.yml` run #92, commit `ce1a8a6`) failed with three cascading errors~~ **Closed same day**: (1) removing `SourceCategory.ALTERNATIVE` from the enum broke deserialization of real, already-persisted `source_registry.json` state (the `production/state-latest` branch) carrying real historical revisions with `category: "alternative"` -- fixed by restoring the enum member (never removing a value real persisted data depends on; only stop emitting new records with it) and adding a direct regression test (`test_legacy_alternative_category_still_deserializes`) plus confirming live by loading the actual persisted registry file. (2) `production.pipeline.ProductionPipeline._stage_investment_case_generator` called `self._tickers(as_of)` (→ `UniverseProvider.constituents(as_of)`, which compares dates) *before* checking `as_of is None` -- any execution whose entire requested range fell on non-trading days (this incident: a single-day scheduled run landing on an EGX Friday) crashed with `TypeError: '<=' not supported between instances of 'datetime.date' and 'NoneType'` instead of skipping cleanly; fixed by moving the check first. (3) `_stage_dashboard_artifact_generator`'s `financial_coverage`/`financial_statements` export passed a bare (possibly-`None`) `as_of` into `build_financial_coverage_report`, which requires a real `date` -- fixed with the same `as_of or end` fallback the same stage already used for `evaluate_publication_gate`/`PortfolioConstructor` immediately above it, just never applied consistently. | Bugs (2)/(3) are genuinely pre-existing (untouched by this session's other changes) and only manifested because this incident's run happened to land on a real EGX non-trading day with an empty date range -- previous live runs and this session's own local testing always exercised at least one trading day, so `as_of` was never actually `None` until now. | Verified live: reproduced all three failures directly against the real persisted `source_registry.json`/production data tree (not assumed), confirmed the fix resolves each one, and added a regression test for each (`test_legacy_alternative_category_still_deserializes`, `test_entire_range_on_non_trading_days_skips_investment_cases_instead_of_crashing`). 736 backend tests pass; `ruff check` clean. Watch the next scheduled `deploy-pages.yml` run (or the next non-trading-day date) to confirm the live fix holds outside local reproduction. |
| 48 | `decision_service.country_risk.SovereignRatingAction` has no real collector yet — `moodys_ratings`/`sp_global_ratings`/`fitch_ratings` are all `PLANNED` in `sources/catalog.py` (feed/RSS endpoint not yet verified). `CountryRiskSeverity.CRISIS` is therefore honestly unreachable in any real run today; only `NORMAL`/`DETERIORATING` (from macro/currency data alone) are ever produced. | Fabricating a rating-action feed or lowering the bar for `CRISIS` to something macro-derived would violate this platform's core anti-fabrication discipline and the exact reasoning `docs/ARCHITECTURE_ADVERSARIAL_REVIEW.md` Section 1.4/1.5 used to design this override in the first place (a lagging, discrete, real event only). | First real collector against one of the three rating-agency press-release feeds goes `IMPLEMENTED` → wire it into a `RssNewsCollector`-style ingestion path producing real `SovereignRatingAction` records, then `CRISIS` becomes genuinely reachable. |
| 50 | ~~`decision_service.country_risk.assess_country_risk()`'s `DEFAULT_CURRENCY_SERIES_ID="EGP_USD"` never matched real production's EGP/USD series id (World Bank's `egypt_official_fx_egp_per_usd`), and FRED (`production/collector_plan.py`'s `LIVE_MACRO_SERIES_IDS`/`live_wired_source_ids`, `acquisition_intelligence/capability.py`'s `MACROECONOMIC` pool) was actively wired into every LIVE run despite timing out in 3 consecutive real live runs, with zero FRED series ever present in real persisted production data~~ **Closed** (project owner direction, 2026-07-31 — "delete dependency on blocked-but-free sources, fix implementation gaps"): (1) `resolve_currency_series()`/`has_sufficient_currency_data()` (new, `decision_service/country_risk.py`) resolve the EGP/USD series against a declared alias list (`CURRENCY_SERIES_ID_CANDIDATES = (EGP_USD, egypt_official_fx_egp_per_usd)`) instead of one hardcoded default; `meta.readiness` now imports this one resolver rather than re-declaring its own currency-id constant — see AD-49. Verified directly against real, already-persisted `production/state-latest` data: `has_sufficient_currency_data()` now returns `True` and `assess_country_risk()` correctly reports `DETERIORATING` (`egypt_official_fx_egp_per_usd moved +8.67%` over the real ~2.5-year lookback window) where it previously silently reported no data at all. (2) `fred` removed from `CAPABILITY_STRATEGIES[Capability.MACROECONOMIC]` and from `live_wired_source_ids()`'s explicitly-wired set (`production/collector_plan.py`), so a real LIVE run no longer spends time/rate-limit budget on a source with a 0% real success rate; `FredCsvCollector`, its `SourceSpec` (still `IMPLEMENTED`, still legally cleared), and its own unit tests are untouched — this is a live-dependency change, not a legal/implementation removal, and is reversible the moment a real fetch succeeds again. (3) `agents.macro._SERIES_MECHANISMS` gained real production series ids (`DCOILBRENTEU`/`egypt_official_fx_egp_per_usd`/etc.) alongside the mock ids, so real findings get a real mechanism sentence instead of the generic `"sensitivity to {series_id}"` fallback. | Both bugs were introduced by this session's own earlier work (the country-risk override and FRED's original live wiring) and only became visible once a real live run's persisted data could be inspected directly — same class of gap as TD-49's incident, caught proactively this time by an investor-perspective review rather than a live failure. | Verified live: reproduced `has_sufficient_currency_data()`'s false-negative directly against real `production/state-latest` macro CSVs (not assumed) and confirmed the fix; confirmed via `git ls-tree` that zero FRED-named series files (`DCOILBRENTEU.csv` etc.) exist anywhere in real persisted production data, corroborating the 3-timeout incident. 741 backend tests pass (5 new: `test_real_production_series_id_is_recognized_not_just_mock_id`, `test_has_sufficient_currency_data_false_when_neither_alias_present`, `test_explicit_currency_series_id_override_still_wins`, `test_investment_horizon_recognizes_real_production_currency_series_id`, `test_fred_excluded_from_live_macroeconomic_pool`); `ruff check` clean. Watch the next scheduled `deploy-pages.yml` run to confirm `country_risk_data_present` now reads `True` live (not just against the extracted archive) and that FRED no longer appears among attempted collectors. |
| 43 | **GDELT evidence-tier gate** (`sources.spec.EvidenceTier`, `collectors.discovery_reconciliation.reconcile_discovery_news()`) is new, real, tested code (project owner direction, 2026-07-29: "GDELT must resolve to a primary source before it counts as evidence") but has never promoted a real row end-to-end against production data: the reprocessed historical batch (TD-41, 284 Egypt-relevant headlines) has every `tickers` column empty (TD-42's false-positive risk made trusting re-resolved tickers unsafe), so 0 rows are currently promotable regardless of how much PRIMARY-source coverage (Enterprise/FRA/Al Borsa/Masrawy/company IR) exists in `news.csv`. The reconciliation stage itself (wired into `ProductionPipeline._stage_event_platform`, runs every `agx run`) is exercised only by unit tests with synthetic ticker-tagged fixtures, never a real end-to-end promotion. | Deliberately shipped without a real promoted example -- fabricating one (e.g. hand-editing a ticker onto a discovery row to "prove" the mechanism) would be exactly the kind of dishonesty this program's rules forbid. The mechanism is real and tested with fakes; only a live scenario (a future GDELT discovery candidate whose ticker is genuinely, unambiguously resolved, corroborated by a real PRIMARY-source report nearby in time) can prove it end-to-end. | Watch `agx reconcile-discovery-news`'s (or the daily pipeline's) promoted/still-pending counts as PRIMARY-source news coverage accumulates in production; the first real promotion is the actual proof this gate works outside of test fakes. Closing TD-42 (safer ticker resolution) would also let a future backfill run populate `news_discovery.csv` with trustworthy ticker attribution instead of leaving it empty. |
| 51 | `meta.readiness.MAX_PRICE_ABOVE_FAIR_VALUE_PCT = 0.20` (how far above the calculated fair value the current price can sit before the new price-quality criterion blocks INVESTMENT readiness — see AD-51) is a declared, uncalibrated threshold, same posture as TD-6/17/20/33/44/45/46. | No real ticker has both a computed fair value and enough decision-ledger history yet to calibrate what price-vs-fair-value gap actually predicts poor real outcomes; picked as a round, conservative starting number, not derived from measured EGX outcomes. | Once enough real INVESTMENT-horizon decisions with a computed fair value accumulate in `meta.decision_ledger.DecisionLedger` → review whether 20% actually separates weak entries from good ones, the same calibration trigger every other declared threshold in this register shares. |
| 52 | `market_memory.breadth.TRAILING_VOLUME_WINDOW_DAYS = 20` (the lookback `compute_market_breadth()` uses to decide whether a ticker's latest volume counts as "above" or "below" average, feeding `market_breadth.json`'s liquidity-breadth counts) is a declared, uncalibrated window, same posture as TD-6/17/20/33/44/45/46/51 — a conventional trading-month length, not derived from any real EGX volume-distribution study. | No real multi-year EGX volume history exists yet to test whether 20 trading days is the window that actually separates a genuine liquidity expansion from routine day-to-day noise. | Once enough real trading history accumulates across more of the universe (the same trigger TD-46's liquidity floor shares) → review whether 20 trading days is the right window, or whether it should vary by ticker/sector liquidity tier. |
| 53 | `production.collector_plan.LIVE_PRICE_LOOKBACK_DAYS = 180` (see AD-52) is a declared window picked with real margin above the 60-observation floor, not a value derived from measuring which window actually maximizes real decision quality — same posture as every other declared-not-calibrated constant in this register (TD-6/17/20/33/44/45/46/51/52). A single shared window for every horizon (MICRO/SWING/INVESTMENT) and every agent (correlation, momentum, technical structure) is also a simplification: a genuinely horizon-tuned window (e.g. a shorter one for MICRO-horizon momentum, a longer one for INVESTMENT-horizon correlation) might separate signal from noise better than one number for all of them. | No real decision-ledger history exists yet across enough real trading days to test whether 180 days (vs. a shorter or longer window, or per-horizon windows) actually produces better real outcomes; picked only to clear `min_observations=60` with margin, not from measured EGX performance. | Once real decision-ledger history accumulates across enough trading days (the same calibration trigger every other declared threshold in this register shares) → review whether 180 days is the right window, and whether per-horizon windows would outperform one shared value. |
| 55 | ~~`valuation.engine.FairValueEngine.value()`'s DDM/P-E models were incorrectly nested inside the `if shares and shares > 0:` gate... only 2 of 101 tickers (ETEL, ORAS) had any financial-statement line items, never `shares_outstanding`~~ **Substantially closed (2026-08-02, "Connect full-universe evidence to CIO decisions" + same-day follow-up review, real production evidence)**: two new `IMPLEMENTED` sources close the `shares_outstanding` gap this debt named as the real blocker — `egxpilot_fundamentals` (`EgxPilotFundamentalsCollector`) derives `shares_outstanding = market_cap / last_price` whenever the API omits it directly, and a real `deploy-pages.yml` run this same day (`production/state-latest`, commit `c3aa5c9`) fetched its market-fundamentals endpoint for all 100 non-AIDC universe tickers successfully; `chief_egx_financials` (`ChiefFinancialsCollector`) independently states `SharesOutstanding`/`total_equity` (`BookValue`) for whichever companies it currently publishes. New `valuation.metrics.compute_valuation_metrics()`/`ValuationMetrics` computes `enterprise_value`, `ev_to_ebitda`, `price_to_book`, `dcf_per_share`, and market P/E/EPS/dividend-yield/beta from these reported fields (never fabricated), exposed on `DecisionReadiness.valuation` and rendered on the Investment Case page's Valuation section. `meta.recommendation_service` now generates a valuation-only INVESTMENT prediction (`multi_model_fair_value`) when no knowledge-weighted model produces one and blends a bounded market-P/E earnings-yield carry into `expected_return`; `meta.readiness.assess_decision_readiness` allows `decision_allowed` from `fair_value_available` alone. Two real, previously-unnoticed defects were found and fixed by this review, both against real evidence, not sandbox guesses: (1) `decision_service.macro_overlay.assess_macro_overlay()` summed raw importance-weight floats into `available_weight` with no rounding (`0.6000000000000001`), breaking a test and risking the same float noise reaching the dashboard artifact — fixed with `round(..., 6)`; (2) inspecting the real `raw_documents.json` this same production run collected showed `ChiefFinancialsCollector.fetch()` only ever walked its company index's page 1 and a hardcoded page 2 (`chiefcapitalco.com/companies/`, `.../page/2/`) — real, only 5 distinct tickers (ARCC/COMI/CIEB/EXPA/ETEL) were actually discovered and fetched that way, an artificial cap on top of whatever Chief Capital genuinely publishes, not a real site limit. Fixed: `fetch()` now walks `page/3/`, `page/4/`, ... until a page adds no new company link or a fetch itself fails (bounded at 20 pages as a runaway guard, not a declared real page count) — same graceful-degradation posture `discovery.engine`'s sitemap-index walk (TD-18) already uses. | The `shares_outstanding`/`total_equity` gap this debt originally named is closed by real, live-evidenced code, but **EV/EBITDA and DCF specifically are not yet broadly reachable**: running `FairValueEngine`/`compute_valuation_metrics()` directly against the real `production/state-latest` financial-statement data (100 tickers with a `financial_statements/*.csv` file) found only **4 of 100 tickers** currently clear the 3-of-7-model fair-value floor (models used: `pe` x4, `residual_income` x4, `pb` x3, `dcf` x2, `ev_ebitda` x1) — `cash_and_equivalents` is missing for **100/100** tickers, `ebitda` for 98/100, `total_debt` for 99/100, `total_equity` for 95/100. This is a genuine, real-evidenced data gap, not a code defect: the real Chief Capital CSVs collected for ARCC/ETEL (real headers quoted directly from `raw_documents.json`: `Year,Assets,Liabilities,BookValue,Revenue,GrossProfit,NetProfit,...,SharesOutstanding,...,P/E,P/BV,ClosingPrice,...`) simply have no `EBITDA`/`TotalDebt`/`Cash` column at all for these companies, and bank CSVs (COMI/CIEB/EXPA) report `CustomerDeposits` instead of `TotalDebt` (correctly not treated as equivalent — deposits are operating funding, not debt, the same reasoning `_is_bank()` already excludes banks from DCF/EPV/EV-EBITDA for); `egxpilot_fundamentals`'s market-snapshot fields (`market_cap`/`market_pe`/`market_eps`/`dividend_yield`/`beta`/`shares_outstanding`) never included balance-sheet detail at all. Inventing a `cash_and_equivalents`/`ebitda`/`total_debt` value for these tickers would be exactly the fabrication this platform's core rule forbids. | The now-unpaginated `ChiefFinancialsCollector` may already surface more than 5 companies (and possibly the missing balance-sheet fields for some of them) on its next real run purely from removing the 2-page cap — the next `deploy-pages.yml` run is the proof; re-run this review's own `FairValueEngine`/`compute_valuation_metrics()` check against the new `production/state-latest` to see the updated 4-of-100 and per-field-missing counts. If coverage stays low after that, the real next step is a *new* free source review specifically for balance-sheet detail (cash, total debt, EBITDA) — this sandbox has no network egress to search for one, so it needs either a live `agx discover-sources` pass or the project owner naming a candidate. |
| 54 | `sources.registry.SourceRegistry.retire_removed()` (see AD-53) only detects a source *deleted outright* from `sources.catalog.seed_sources()` — it has no way to know when a still-catalogued source's own capability mapping in `acquisition_intelligence.capability.CAPABILITY_STRATEGIES` was removed instead (e.g. `fred`, still `IMPLEMENTED` and still in the catalog, but deliberately excluded from the live macroeconomic pool per TD-50). That's the correct behavior for `fred` specifically (the code is real and meant to be kept, per TD-50's own reasoning) but means a future source that's fully abandoned (removed from every capability's candidate pool, never re-added, with no consumer left) but *not* deleted from the catalog would stay `PLANNED`/`IMPLEMENTED` and visibly "live" forever, same failure mode this debt's own fix just closed for outright-deleted sources. | Distinguishing "still catalogued but capability-orphaned" from "genuinely still useful but currently de-prioritized" (`fred`'s case) requires a judgment call this mechanism shouldn't make automatically — silently retiring a source someone deliberately kept catalogued (for its tested collector code, or as a documented fallback) would be a real regression, not a cleanup. | If a future source-registry review finds a catalogued-but-capability-orphaned source with no plausible future use (unlike `fred`), retire it explicitly via the same `retire_removed`-style revision rather than leaving it to accumulate silently — or, if this becomes a recurring pattern, extend `retire_removed` to also flag (not auto-retire) sources absent from every `CAPABILITY_STRATEGIES` pool for a maintainer to review. |
| 56 | `market_memory.regime.REGIME_LOOKBACK_DAYS = 20` and its bullish/bearish/elevated/high-volatility thresholds (`market_memory/regime.py`) are declared, uncalibrated starting values, same posture as every other declared-not-measured constant in this register (TD-6/17/20/33/44/45/46/51/52/53). | No real multi-year EGX regime history exists yet to test whether ±3% cumulative return and 1.5%/2.5% daily volatility actually separate a genuine trend/volatility shift from routine noise. | Once real decision-ledger history and enough real trading days accumulate (the same calibration trigger every other declared threshold in this register shares) → review whether these thresholds correctly anticipate periods that mattered for real decision outcomes. |
| 57 | `learning.monitor.ContinuousLearningMonitor` (thesis-failure detection, Q8 of the Institutional Investment Validation mission) is real, mechanically correct, and proven so by `institutional_validation`'s own repeatable scenario (a synthetic declining thesis is correctly retired after 3 monitored records; a synthetic confirming thesis is correctly never retired) — but it is not wired into `production.pipeline.ProductionPipeline`. No autonomous `agx run` today ever invokes thesis-failure detection; it is only exercised directly, in tests, and now in the validation framework. | Wiring it in needs a real cadence decision this validation mission deliberately did not make unilaterally: it only produces a usable realized-return measurement once `as_of` is meaningfully later than a knowledge object's `discovery_date` (enough trading days for the horizon's forward-return window to have actually elapsed), which interacts with the daily pipeline's existing per-day determinism guarantee in a way that needs its own design pass, not a quick bolt-on. | A dedicated design pass on when (daily? weekly? only when new price data crosses a horizon's forward window past a knowledge object's discovery date?) `ContinuousLearningMonitor.evaluate()` should run inside `ProductionPipeline`, then wire it in as its own stage. |
| 58 | `meta.decision_ledger.DecisionLedger.evaluate_expired()`'s benchmark-comparison math (Q7) is proven correct by `institutional_validation`'s own synthetic scenario (hand-computed excess return matches exactly, net of transaction costs), but no ticker literally named `EGX30` (the default `benchmark_ticker`) — or any other index-level price series — exists anywhere in real mock or production price data. Every real `DecisionRecord`'s `benchmark_return`/`excess_return`/`hit` therefore stays `None` today. | This is the same root cause as the platform's broader lack of a licensed/free EGX index-level feed (`docs/ACQUISITION_STRATEGY.md`'s Price Data Feasibility Mission, AD-32) — the benchmark mechanism only needs *a* price series registered under the `benchmark_ticker` id, which is exactly what's missing, not a code defect. | Once a real EGX30 index-level price series is legally obtainable and collected under the `EGX30` ticker id (or `evaluate_expired()` is called with whatever id it's actually collected under), real benchmark-evaluated decisions become possible with no code change. |
| 59 | `DecisionService.decide_portfolio()` never referenced `HorizonDecision.max_position_pct` in its `target_weight` formula (only capped at `self.max_position_weight`), and `agx decide`/`cli.py` never called `meta.publication_gate.apply_publication_gate()` at all before invoking `decide_portfolio()` — found (Investment Proof Framework mission, 2026-08-01) by directly exercising the CLI decision path the same way this mission's attribution/counterfactual engines exercise the research path. Together these meant identical evidence sized a position ~6x larger through the position-aware path than through `PortfolioConstructor` (which already applied `max_position_pct` correctly), and every real `agx decide`/Decision Center call always produced `no_action`/zero weight with no reason naming the real cause (a Rule-5 explainability defect, not just a sizing one). **Fixed**: `decide_portfolio()`'s `target_weight` now takes the same three-way `min()` (`score`-derived share, `self.max_position_weight`, `decision.max_position_pct`) `PortfolioConstructor.construct()` already used; a new `elif decision.publication_status != PublicationStatus.PUBLICATION_READY` branch surfaces the real gate blockers as `reasons`/`abstained=True` instead of a silent zero; `cli.py` gained a shared `build_publication_gate_report()` helper (deduped from `publication-status`'s own inline logic) and the `decide` command now calls `apply_publication_gate()` before `decide_portfolio()`, matching what `institutional_validation.scenarios._force_publication_ready()` already had to do for its own scenarios. See AD-55. | The zero-weight *outcome* itself is correct today (`docs/DECISION_SYSTEM_ACCEPTANCE.md`'s "research-only decisions display zero position" requirement — no real EGX vendor is licensed yet) — the defect was the missing `max_position_pct` cap (a real sizing bug, reachable the moment publication readiness clears) and the missing explanation (reachable today, on every call). | Verified directly: built a real `macro_agent` `KnowledgeObject` → `BUY_CANDIDATE` recommendation → ran the real gate with today's actual blocker → confirmed `DecisionService` now reports `action=no_action, target_weight=0.0, abstained=True, reasons=["Live, legally usable EGX market data: condition or evidence reference is incomplete."]` instead of an unexplained zero. 6 `test_decision_service.py` fixtures updated to build publication-ready recommendations via the real `apply_publication_gate()` rather than hand-setting `publication_status`. 833 backend tests pass; `ruff check` clean. |
| 60 | `investment_proof.committee_validation.CommitteeValidationEngine`'s `historical_usefulness_status` is honestly `"ready_for_data"` for every committee (Macro/Sector/Quality/Value/Catalyst/Technical) rather than a computed correlation between a committee's opinion and later realized outcomes. | Computing it needs `meta.decision_ledger.DecisionRecord` to know which evidence categories actually contributed to each recorded decision, and it does not — `DecisionRecord` carries the final blended prediction, not a per-category attribution breakdown. | Once `DecisionRecord` (or a new linked artifact) carries per-category attribution at record time — a real schema change, not something this mission should make unilaterally mid-framework — `CommitteeValidationEngine` can correlate each committee's historical directional calls against `DecisionRecord.hit`/`excess_return` once enough real, benchmark-evaluated records exist (same 30-record floor `ConfidenceCalibrationFramework`/`DecisionLedger.performance_summary()` already use). |
| 61 | The IOS mission's mandatory product audit (2026-08-01) confirmed `DashboardDataProvider.getRuntimeStatus()`/`getSourceTruth()`/`getEndpointCandidates()` (three real, tested artifact getters, all three implementations) are called by zero pages even after the full navigation redesign — the same gap the audit was designed to catch, named here rather than silently left. `getRuntimeStatus()`/`getEndpointCandidates()` duplicate ground already covered on Settings (Runtime & Versions, Weekly Discovery) at finer grain than a CIO-facing screen needs; `getSourceTruth()` (per-source ground-truth reconciliation) has no consuming UI concept yet anywhere in the IA. | None of the three fail the Product Law's "does this improve an investment decision?" test on their own — they are operational/source-trust detail, not decision input — so adding a screen for them would violate the same mission's "no page exists solely to display data without contributing to a decision" acceptance criterion. Removing the (real, correct) provider methods themselves would be pure churn with no product benefit. | If a future System & Operations (Settings) sub-view is ever justified by a real maintainer need (e.g. reconciling source ground-truth during a data-quality incident), wire these three in there first — they need no new backend work, only a consuming component. Until then, leave as dead-from-the-UI, live-in-the-provider, exactly as named here rather than silently. |
| 62 | The Capital Allocation Intelligence mission (2026-08-01) wired the new `CapitalAllocationEngine`/`POST /capital-allocation`/`postCapitalAllocation()` stack into CIO Desk only — `Portfolio.tsx` (the dedicated holdings-entry page, `/portfolio`) still renders only the plain `PositionAwareDecision` table it had before this mission, with no deployment-queue/opportunity-cost/recycling view of its own, even though it already calls `postDecisions()` with the exact same request `postCapitalAllocation()` needs. | CIO Desk ("what should I do today?") was the mission's explicit target; duplicating the same 7-section view onto Portfolio in the same pass would have meant designing a second page-specific layout for identical data under real time pressure, risking a rushed, inconsistent second copy rather than one considered one. | Extend `Portfolio.tsx` with the same `CapitalAllocationPlan` view (or a link to a shared component CIO Desk's Capital Allocation section already renders) once a real product pass considers whether Portfolio's own layout (a two-pane holdings-editor + decision-table page) suits the queue/recycling format as-is, or needs its own arrangement. |
| 63 | `research/data/production/provenance_index.json` (`collectors.provenance_index.ProvenanceIndexRepository` — one record per artifact_type/key/record_date, so it grows with every ticker's every collected day) crossed GitHub's *hard* 100 MiB per-blob limit for the first time on 2026-08-01 (measured at 101.48 MiB), silently rejecting `deploy-pages.yml`'s push to `production/state-latest` (pre-receive hook decline) and, because that step has no `continue-on-error`, skipping every step after it — including the actual `npm run build -w web`/`upload-pages-artifact`/deploy steps, so the site never redeployed that run despite the research pipeline itself succeeding. Fixed same-day with the identical gzip-before-commit/gunzip-on-restore treatment `raw_documents.json` already had (highly compressible JSON, same pattern, same two workflow steps). | gzip buys real headroom (the same file's sibling, gzipped `raw_documents.json`, only trips GitHub's *soft* 50 MiB warning at a much larger uncompressed size) but does not cap `provenance_index.json`'s underlying growth — one record per ticker per collected day accumulates forever, with no pruning/rotation mechanism anywhere in `ProvenanceIndexRepository`. | Once real production history spans enough calendar time that even the gzipped file approaches a new size ceiling, `ProvenanceIndexRepository` needs an actual retention policy (e.g. drop or archive-elsewhere records older than N days/the oldest DecisionLedger evaluation window still needs) — a real architectural decision, not something to make unilaterally while unblocking a failed deploy. Watch `deploy-pages.yml`'s "Persist updated production state" step size over time as the trigger. |
| 64 | New `shadow_fund/` package (AD-56): three real, honestly-open gaps named at ship time rather than silently left. (1) `benchmark_nav`/`benchmark_cumulative_return_pct` stay flat at the inception value for every real run today — same root cause as TD-58 (`DecisionLedger`'s own benchmark comparison): no ticker literally named `EGX30` (or any index-level series) exists anywhere in real collected price data, so `compute_adjusted_closes(snapshot.price_history.get("EGX30", []), ...)` returns an empty series and `_latest_price_on_or_before` always returns `None`. (2) `REBALANCE_THRESHOLD_PCT = 0.01`/`MIN_RISK_SAMPLE_DAYS = 20` are declared-not-calibrated starting values, same posture as every other declared threshold in this register (TD-6/17/20/33/44/45/46/51/52/53/56). (3) The web Monitoring section renders NAV/risk as stat tiles and an open-positions/transactions table, not a NAV-over-time line chart — the full `shadow_fund_history.json` NAV series is already exported and fetched (`getShadowFundHistory()`), just not yet visualized as a curve. | (1) is the exact same missing-index-feed gap `docs/ACQUISITION_STRATEGY.md`'s Price Data Feasibility Mission (AD-32) already names — not a new blocker, and the fund's own cumulative return is already real and correct even while the benchmark side stays flat. (2)/(3) are scoping choices made to ship the core persistent-portfolio-state architecture (the mission's actual ask) in one pass rather than also tuning thresholds with no real calibration data yet, or building a charting component under the same time pressure. | (1): once a real EGX30 (or other) index-level price series is legally obtainable and collected under the `benchmark_ticker` id, real benchmark/excess-return numbers become possible with zero code change — same trigger as TD-58. (2): once real Shadow Fund NAV history accumulates across enough trading days, review whether 1% and 20 days are the values that actually separate genuine rebalances/a meaningful sample from noise, the same calibration trigger every other declared threshold in this register shares. (3): a NAV-history line chart on Monitoring is a scoped, low-risk follow-up — the data contract (`ShadowFundNavPoint[]`) already exists, only a chart component is missing. |
| 66 | ~~`scripts/build_financial_source_registry.py` only ever consulted the static `egx30_web_search_domain_hints.json` snapshot for a company's website hint -- `discovery.wikidata_lookup.WikidataOfficialWebsiteClient` existed, was tested, and was already wired into `cli.py discover-sources`, but was never called here, so a company whose only real hint was a Wikidata `P856` claim was recorded `HOMEPAGE_UNRESOLVED` in this specific registry regardless. Separately, `cli.py discover-sources` itself picked exactly one hint per company by source priority and discarded the other, even though `TargetOrganization.domain_hints` is already a list and `HeuristicDomainResolver.candidate_domains()` already tries every hint in order before falling back to a heuristic guess -- a company's top-priority hint being unreachable silently lost the fallback try at its own second, independent hint.~~ **Closed (2026-08-03, EGX Entity Resolution Engine mission)**: `discovery.company_entity_resolution.EntityResolutionEngine` runs every configured website-hint strategy and keeps every result (`resolve_website_candidates()`), instead of one strategy silently winning; both real call sites (`build_financial_source_registry.py`, `cli.py discover-sources`) now build one `EntityResolutionEngine` and pass the combined, confidence-ordered candidate list into the same, unmodified `HeuristicDomainResolver`. `discovery.gleif_lookup.GleifLegalEntityClient` (the Global LEI Foundation's free, no-key public registry) is a new, independent strategy resolving a capability this codebase had never had before: a company's canonical legal name and known aliases (`CompanyFinancialSourceRecord.legal_name`/`aliases`/`lei`, new optional fields), via its fuzzy-completions name search matched with the same `significant_tokens` token-overlap discipline `wikidata_lookup`/`discover_company_directory_links` already use. See AD-62. | The GLEIF fuzzy-completions/lei-records API contract encoded in `gleif_lookup.py` is this module's best-documented understanding of GLEIF's public JSON:API, not something fetched live from within this development sandbox (no outbound egress here -- same honesty posture as `wikidata_lookup.py`'s own module docstring, and the same posture `AD-24` requires of any hint source: nothing is trusted until independently re-verified). | First real `discover-sources`/`build_financial_source_registry.py` run with live egress (e.g. the next scheduled `discovery.yml`) is the actual proof: confirm `GleifLegalEntityClient` produces real matches against real Egyptian-issuer LEI records (not just its own unit-test fixtures), and confirm the multi-strategy website merge measurably raises the real `website_resolution_rate` reported in `company_financial_sources_summary.json` versus the prior single-strategy run. If GLEIF's real Egyptian-issuer coverage turns out low, that is itself useful, honestly-reported information, not a reason to lower the match-confidence bar. |
| 65 | The new per-decision Decision Quality gate (AD-58, `meta.decision_quality`) has not yet been exercised against a real `agx run`/`deploy-pages.yml` cycle -- unit-tested thoroughly (`test_decision_quality.py`) and confirmed working against every existing synthetic-scenario framework's real fixtures, but how many of the real universe's tickers actually reach `PUBLICATION_READY` under real, live-collected `Explanation`s (real agents' thesis/evidence text, real `ProvenanceRef`s) is not yet known. | The gate's logic is real and tested; what's unverified is real-world coverage, the same category of honest gap every "not yet run live" item in this register shares (e.g. TD-39's registry-coverage claim). | The next scheduled `deploy-pages.yml` run is the real proof -- read the resulting `investment_cases.json` to see how many `HorizonDecision`s reach `PUBLICATION_READY` and, for the ones that don't, which of the six quality checks blocked them; if a real agent's `Explanation` construction turns out to leave `evidence_refs`/`invalidation_conditions` empty in practice (unlike this session's synthetic fixtures, which were updated to populate them), that agent's construction is the real follow-up fix, not the gate. |
| 67 | New `discovery.resolution_memory.ResolutionMemory` (AD-63) persists every entity-resolution `Observation` (a proposed website candidate, a probe result, a GLEIF match/miss) and synthesizes `Claim`s from them, but each `ResolutionMemoryRecord` version only holds the observations its own run produced -- it does not yet carry prior versions' observations forward so a synthesis pass sees full cross-run history, and does not yet use that history to change resolution *behavior* (a domain refuted as unreachable in run N is still re-probed from scratch in run N+1, the same "always re-attempt anything not `VALIDATED`" posture `CompanyFinancialSourceRegistry.is_resumable_skip()` already has at the company level, just not yet extended to the claim level). Separately, `ClaimAttribute.BRAND_NAME`/`PARENT_COMPANY` are reserved enum members with no populating strategy anywhere in this codebase yet -- no `Observation`/`Claim` of either attribute exists today. | Cross-run observation accumulation and skipping known-refuted domains are both resolution-*algorithm* changes (what gets probed, and when), deliberately out of scope for a mission scoped to modeling institutional knowledge, not changing behavior (see AD-63's rationale). The reserved attributes exist so a real future source (e.g. a GLEIF direct-parent-relationship lookup, or a dedicated brand-name strategy) has somewhere honest to write to, in the exact same `Observation`/`Claim` shape every other attribute already uses -- same posture as `patterns.json` staying `[]` until a dedicated `Pattern` model exists (TD-15). | A follow-up pass that (1) loads `ResolutionMemory.latest(ticker).observations` before building a new run's record and appends this run's new observations to it rather than starting empty, then (2) re-synthesizes `claims` from the full accumulated set, then (3) skips re-probing a domain whose most recent observation already refuted it (likely with a TTL, the same "might succeed later" reasoning `is_resumable_skip()` already applies at the company level) turns this from pure institutional knowledge into an actual "never repeat work" optimization. A real GLEIF direct-parent lookup or brand-name source producing `BRAND_NAME`/`PARENT_COMPANY` claims is the trigger for those specifically. |

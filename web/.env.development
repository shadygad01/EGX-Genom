# Local dev: talk to a running `npm run dev -w api` through Vite's /api proxy
# (see vite.config.ts). Switch to `static` to develop against
# web/public/data/*.json instead, with no backend running.
VITE_DATA_PROVIDER=api

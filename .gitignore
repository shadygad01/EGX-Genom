# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/
.uv/

# Node
node_modules/
dist/
.vite/
*.tsbuildinfo

# Editors / OS
.vscode/
.idea/
.DS_Store

# Env / secrets
.env
.env.*
!.env.example
# Non-secret, checked-in Vite mode config (no credentials; see web/.env.production
# and web/.env.development) -- everything else matching .env.* stays ignored.
!web/.env.production
!web/.env.development

# Generated dashboard artifacts (agx_research.cli export-dashboard writes these
# fresh in CI before the Pages build; see .github/workflows/deploy-pages.yml)
# web/public/data/ intentionally committed for static GitHub Pages distribution

# Logs
*.log
